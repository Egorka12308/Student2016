clear
B = [];
T = [];
y = [];
%Параметры для экспоненциальной функции
Lt = 100;%Предел определения функции
St = 1;%Шаг аргумента
A = 1;
t1 = 16;
t2 = 5;
%Параметры для фильnра
K = 6;
L = 6;
M = 16;
%Вызов функции и отрисовка в первом окне
[T] = 0:St:Lt;
[B] = func_exp(A, T, t1, t2);
plot(T, B);
%Включаем свой фильтр и рисуем его в новом окне
for t = 0:100
    y = [y, triangle_filter(t, t1, t2, K, L, M)];
end
figure
plot(T, y);