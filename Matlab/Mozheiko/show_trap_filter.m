clearvars 
t = 0:1:100 %задаем нижний предел, шаг и верхний предел - генерируем массив 
y1 = func_exp(16, 5, t)  % получаем функцию с заданными значениями
plot(t,y1) %строим график

y2 = [];
for i=0:100
    y2 = [y2 trap_filter(8, 5, 16, 16, 5, i)]; % по оси ординат
end
figure
plot(t,y2)